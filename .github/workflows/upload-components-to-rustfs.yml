name: Upload changed components files to RustFS

on:
  push:
    branches:
      - main
    paths:
      - 'components/**'

jobs:
  upload-components:
    runs-on: ubuntu-latest

    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.S3_ACCESS_KEY }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.S3_SECRET_KEY }}
      AWS_DEFAULT_REGION: us-east-1
      S3_ENDPOINT: ${{ secrets.S3_ENDPOINT }}
      S3_BUCKET: ${{ github.event.repository.name }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install AWS CLI
        run: |
          aws --version || true
          if ! command -v aws >/dev/null 2>&1; then
            curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
            unzip -q awscliv2.zip
            sudo ./aws/install
          fi
          aws --version

      - name: Prepare bucket name
        id: bucket
        shell: bash
        run: |
          set -e

          REPO_NAME="${{ github.event.repository.name }}"
          BUCKET_NAME=$(echo "$REPO_NAME" | tr '[:upper:]' '[:lower:]')

          echo "Repository name : $REPO_NAME"
          echo "Bucket name     : $BUCKET_NAME"

          echo "bucket_name=$BUCKET_NAME" >> "$GITHUB_OUTPUT"

      - name: Create bucket if not exists
        shell: bash
        run: |
          set -e

          if aws s3api head-bucket \
            --bucket "$S3_BUCKET" \
            --endpoint-url "$S3_ENDPOINT" 2>/dev/null; then
            echo "Bucket $S3_BUCKET already exists"
          else
            echo "Creating bucket $S3_BUCKET"
            aws s3api create-bucket \
              --bucket "$S3_BUCKET" \
              --endpoint-url "$S3_ENDPOINT"
          fi

      - name: Find changed files in components
        id: changed
        shell: bash
        run: |
          set -e

          BEFORE="${{ github.event.before }}"
          AFTER="${{ github.sha }}"

          echo "Before: $BEFORE"
          echo "After : $AFTER"

          if [ -z "$BEFORE" ] || [ "$BEFORE" = "0000000000000000000000000000000000000000" ]; then
            echo "Initial push detected"
            git ls-files components > changed_files.txt || true
          else
            git diff --name-only --diff-filter=ACMRT "$BEFORE" "$AFTER" -- components > changed_files.txt || true
          fi

          # Chỉ giữ các file có extension cho phép
          grep -E '\.(txt|js|png)$' changed_files.txt > filtered_files.txt || true
          mv filtered_files.txt changed_files.txt

          echo "Changed files to upload:"
          cat changed_files.txt || true

          if [ ! -s changed_files.txt ]; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_changes=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Upload changed files
        if: steps.changed.outputs.has_changes == 'true'
        shell: bash
        run: |
          set -e

          BUCKET_NAME="${{ steps.bucket.outputs.bucket_name }}"

          while IFS= read -r file; do
            [ -f "$file" ] || continue

            ext="${file##*.}"
            ext_lower=$(echo "$ext" | tr '[:upper:]' '[:lower:]')

            case "$ext_lower" in
              js)
                content_type="text/javascript"
                ;;
              png)
                content_type="image/png"
                ;;
              txt)
                content_type="text/plain"
                ;;
              *)
                echo "Skip unsupported file type: $file"
                continue
                ;;
            esac

            echo "Uploading: $file -> s3://$BUCKET_NAME/$file (Content-Type: $content_type)"

            aws s3 cp "$file" "s3://$BUCKET_NAME/$file" \
              --endpoint-url "$S3_ENDPOINT" \
              --content-type "$content_type" \
              --only-show-errors
          done < changed_files.txt
